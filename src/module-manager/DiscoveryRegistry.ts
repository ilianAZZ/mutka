// The DiscoveryRegistry now lives in core (sources are contributed by module
// runtimes). Re-exported here so existing UI importers keep working.
export { DiscoveryRegistry } from "../core/discovery/DiscoveryRegistry";

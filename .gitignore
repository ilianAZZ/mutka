node_modules
dist/

# Reference/example dev modules ARE tracked (they exercise the worker runtime and
# document the module API; they're DEV-only and tree-shaken out of release builds).
# Keep only the experimental, not-for-publication one local:
dev-modules/com.cloud-status
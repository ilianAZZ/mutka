---
bump: minor
---

Add CLI support: `mutka <path>` to navigate, `--picker` to pick a file (outputs to stdout), `--run <action>` to execute module actions, `--list-actions` to list available actions. Single-instance support forwards CLI args to the running app.

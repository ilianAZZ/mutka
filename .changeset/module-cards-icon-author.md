---
bump: minor
---

Modules catalog: show one card per module (multi-module repos via mutka.config.json `projects`), each with an image and author. Modules declare `icon` + `author` in defineModule; the author's GitHub avatar/profile defaults to the repo owner. Redesigned the installed + browse cards.
